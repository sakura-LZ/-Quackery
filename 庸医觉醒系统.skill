# 庸医觉醒系统项目地图

## 项目目的与当前状态
- 目的：交付一个无需后端、可静态部署的单机临床轮转叙事游戏，面向喜欢轻策略、成长与选择驱动故事的玩家。
- 当前阶段：MVP 功能与内容已基本成型，处于“整体验收、方向确认与发布准备”阶段。
- 核心旅程：进入系统 -> 在六科完成 30 个虚构化病例 -> 在半程/完成节点抽取技能 -> 专业指标、医德与伤患共同决定结局 -> 本地保存或重置进度。
- 当前入口：`index.html`；没有构建步骤、后端或包管理器。
- 工作源目录：`D:\小游戏\庸医觉醒系统`，当前不是 Git 仓库。
- 发布仓库：`D:\小游戏\-Quackery`，连接 GitHub/Vercel，Git 主分支为 `main`。

## 双目录同步规则
- 所有在工作源目录确认完成的代码、资产和治理文档改动，都必须同步到 `D:\小游戏\-Quackery` 的对应相对路径。
- 同步方向默认为“工作源目录 -> 发布仓库”；若发布仓库存在更新、更晚或独有改动，先比较差异，不得用源目录版本直接覆盖。
- 每次同步前先运行 `git -C 'D:\小游戏\-Quackery' status --short --branch`，保留用户未提交改动并只同步本批涉及文件。
- 同步后在两个目录运行与改动相称的相同验证；发布仓库的 Git 状态用于确认实际待提交文件。
- 不自动执行 `git commit`、`git push` 或触发 Vercel 发布，除非用户明确要求。
- 发布仓库中的 `PRODUCT.md` 是精简产品说明，应与本项目的定位和红线保持一致，但不替代完整治理文档。

## 先读文件
- 产品范围或阶段判断：`项目定位和设计方案.md`、`问题总结.md`。
- 玩法与状态机：`game.js`，再按需读取 `depts/<科室>.js`。
- 叙事与措辞：`叙事设计文档.md`。
- 视觉与资产集成：`overview.md`、`draft_styleguide.md`、`.workbuddy/visual_style_v2.md`、`art.js`。
- 音频：`音频设计文档.md`、`audio.js`。
- 数值平衡：`balance/report.md`、`balance_sim.js`。
- 历史 MVP 计划：`docs/superpowers/plans/2026-08-30-庸医觉醒系统-mvp.md`，仅作历史记录，不代表当前状态。

## 主要代码边界
- `index.html`：页面语义结构、资源加载顺序和应用挂载节点。
- `game.js`：基础科室数据、状态机、渲染、存档、技能、结局与测试钩子。
- `depts/*.js`：六科扩展病例与长对话，通过 `window.YONGYI_DEPTS` 合并到核心数据。
- `styles.css`：主界面视觉、布局与响应式样式。
- `cinematics.js` / `cinematics.css`：序章、转折、抽卡与结局演出。
- `art.js` / `assets/`：程序化降级图、美术注入和 16 个主位图资产。
- `audio.js`：Web Audio 音效、音乐与科室氛围控制。
- `balance_sim.js` / `balance/report.md`：无头全局流程模拟及生成报告。
- `depts/validate.js`：30 个病例的节点、分支和决策结构验证。

## 数据、输出与临时文件
- 浏览器存档键：`yongyi_save_v1`，仅写入当前浏览器 `localStorage`。
- 正式生成输出：`balance/report.md`。
- 过程验证输出：根目录和 `depts/` 下的 `_*.txt`、`*_out.txt`、诊断脚本；除非任务明确涉及诊断历史，否则不要批量读取或清理。
- 资产临时目录：`assets/**/_tmp_*`；不要扫描、删除或覆盖，除非正在处理对应资产且获得确认。

## 启动与验证
```powershell
Set-Location 'D:\小游戏\庸医觉醒系统'
python -m http.server 4173
```

浏览器打开 `http://127.0.0.1:4173/`。

```powershell
# 全部 JavaScript 语法检查
$files = Get-ChildItem -Recurse -File -Filter '*.js'
foreach ($file in $files) { node --check $file.FullName }

# 病例对话结构检查
node depts\validate.js

# 数值与结局模拟；会重写 balance/report.md，运行前检查是否允许更新报告
node balance_sim.js

# 自动虚拟玩法比较契约；会先运行模拟器，再校验全部策略与目标指标
node verify_balance_profiles.js
```

浏览器验收至少覆盖：首次进入、切换科室、完成病例、50%/100% 抽技能、刷新恢复、重置确认、音频开关、过场、多结局，以及桌面/移动端无明显遮挡。

## 架构约束与红线
- 保持原生 HTML/CSS/JavaScript 与静态部署，除非用户明确批准技术栈迁移。
- 不接账号、后端、云存档、分析统计或真实患者数据；不上传浏览器存档。
- 病例是游戏化、虚构化内容，不构成医疗建议；未经专业审校，不宣称医学准确性或教学资质。
- 不未经确认删除/覆盖用户资产、诊断历史、存档兼容逻辑或大范围重做视觉。
- 不把 AI 美术资产的版权、授权或可商用性视为已经确认。
- 当前页面会访问 Google Fonts，与“完全离线/不访问第三方服务”的旧约束冲突；在用户决定前不得把项目描述为完全离线。

## 易损点
- `index.html` 中脚本顺序决定 `window.YONGYI_DEPTS`、`GameAudio`、`Cinematics` 和 `Art` 的可用时机。
- `game.js` 使用 `innerHTML` 重绘患者区，`art.js` 依赖 `MutationObserver` 重新注入资产。
- `balance_sim.js` 从 `game.js` 抽取函数和数据文本；重命名、改写函数形态或拆分数据可能使模拟器失效。
- 数值或选项逻辑改动完成后必须运行 `verify_balance_profiles.js`；自动模拟须包含胜任力均衡路线以证明隐藏结局可达。正式输出包括 `balance/report.md` 和 `balance/latest.json`，两者应同步到发布仓库。
- 技能阈值与科室病例总数绑定；调整病例数量时必须同时验证 50%/100% 抽取和存档兼容。
- 存档键为 `yongyi_save_v1`；状态结构变更需要迁移策略，不能直接假设旧档可读。读取带有短暂 `relations` 字段的旧档时应删除该字段，保留其余进度。
- 选项结算必须保持多维取舍：医学/学业/实操不能按医德正负模板联动；运行状态不包含人情世故；全量选择应至少包含一项收益与一项代价，并至少影响临床思维或专业技能之一。
- 尊重、倾听、解释、告知、安抚与共同决策不增加专业技能；只有实际查体、抢救、穿刺、手术、用药执行等临床操作可以增加专业技能。
- 玩家可见医德必须限制在 0–100、初始 50；内部累计倾向不得直接作为 HUD、JSON 或报告中的医德分展示。
- 绩点状态必须保持在 0–4，临床思维和专业技能必须保持在 0–100；选择、风险、技能和旧档迁移均须执行同一归一规则。
- 隐藏结局除零伤患、高医德和技能积累外，还要求临床思维与专业技能均达到 70，防止单一维度刷满绕过胜任力要求。
- 后续病例优先覆盖 DRG/支付改革、集采、无力支付、医患冲突、红包、无理取闹和患者携 AI 结论问诊；新增内容仍须遵守多维取舍规则。

## 当前阶段出口标准
- 核心用户旅程可在浏览器端完整复现，无阻塞错误。
- 30 个病例结构验证通过，六科、技能抽取、存档和结局均有实际交互证据。
- 视觉、音频、外部字体、医疗表述和发布范围的待确认事项已关闭或明确延期。
- 已列出剩余限制，并在 `D:\小游戏\-Quackery` 建立可追踪的发布检查点。
